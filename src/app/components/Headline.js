import styled from 'styled-components'

const Primary = styled.h1`
  font-family: 'Dosis', sans-serif;
  font-weight: 700;

  margin: 0;
  padding: 0;

  font-size: 2em;
  line-height: 2.5em;
`

const Secondary = styled.h2`
  font-family: 'Dosis', sans-serif;
  font-weight: 500;

  margin: 0;
  padding: 0;

  font-size: 1.6em;
  line-height: 2em;
`

export const Headline = { Primary, Secondary }
